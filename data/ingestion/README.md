# OFFMAP Ingestion

Python scraping and API ingestion code belongs here.

- `scrapers/`: source-specific collectors
- `pipelines/`: normalization, enrichment, and load steps
- `requirements.txt`: Python dependencies for ingestion jobs

Keep app runtime code out of this folder.
